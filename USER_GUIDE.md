# USER GUIDE · html-editor 使用手册

面向不会前端的人。看完这份你就能像用 Word 一样改任何 HTML。

---

## 1. 打开编辑器

**方式 A：让 AI 帮你打开**（在 QoderWork 里最方便）

在对话里说：

> 编辑这份 HTML：`~/Desktop/xxx.html`

或者：

> 让 `~/Desktop/xxx.html` 可编辑

AI 会自动跑注入脚本 → 生成 `xxx.editable.html`（**不动原文件**）→ 用你的默认浏览器打开。

**方式 B：自己一行命令**

```bash
python3 <安装路径>/assets/inject.py 你的文件.html
```

生成同目录下 `你的文件.editable.html`，双击它即可。

**方式 C：拖到 install.sh 装完之后**

装到 QoderWork 后，任何时候说"用 html-editor 编辑 xxx"就行。

---

## 2. 编辑操作速查

打开后可编辑副本，顶部会看到一条深色工具栏，页面自动进入**编辑模式**。

### 2.1 改文字

直接鼠标点任何一段文字（标题、段落、表格单元格、列表项），光标出现就能改。跟改 Word 一样。

**注意**：按钮、tab、导航链接等交互元素故意不可编辑，避免误点破坏功能。想改这些的文字要用区块复制/删除的方式重来。

### 2.2 改字体、字号、颜色、加粗、斜体、下划线、链接

先**用鼠标选中一段文字**（或点进一个文本元素里），顶部工具栏会自动出现格式化条：

| 按钮 | 作用 | 快捷键 |
|---|---|---|
| 字体下拉框 | 换字体（PingFang SC / Songti SC / 微软雅黑 / Arial / Times 等） | — |
| A- / A+ | 减小 / 增大字号 | — |
| **B** | 加粗 | `⌘/Ctrl + B` |
| *I* | 斜体 | `⌘/Ctrl + I` |
| U | 下划线 | `⌘/Ctrl + U` |
| 🎨 | 文字颜色 | — |
| 🔗 | 添加/修改链接 | — |

字号和字体写成**元素的内联样式**，所以导出后不依赖原页面的 CSS，跨环境保留。

### 2.3 拖区块换位（核心功能）

鼠标悬到某个"区块"上，右上角会出现一个 `⠿` 六点手柄。

- **按住手柄拖动** → 页面出现蓝色虚线占位 → 松手即完成换位
- **Alt + 点击区块** → 选中区块（不进入文字编辑）
- 选中区块后顶部工具栏出现 `↑ ↓ ⧉ ✕ ＋ 🖼` 按钮：
  - `↑ ↓` 上移 / 下移一格
  - `⧉` 复制该区块
  - `✕` 删除
  - `＋` 在下面新增一个空区块
  - `🖼` 换图片（区块里有 `<img>` 时可用）

**"区块"是什么？** 编辑器会自动识别页面的顶层结构单元——通常是 `<body>` 或某个 `.container` / `.page` / `main` 的直接子元素，比如 header、每个 section、footer、每张卡片、每个图表容器。你不用管细节，鼠标悬上去能出手柄的就是可拖的。

### 2.4 换图片

- **方式 1**：Alt+点包含图片的区块 → 顶栏 `🖼` → 选本地图片
- **方式 2**：直接点图片本身（如果它单独是个区块）

选完图片会被自动转成 **base64 内联**，导出后不依赖任何外链，发给别人也能看到。

### 2.5 撤销 / 重做

| 操作 | 快捷键 |
|---|---|
| 撤销 | `⌘/Ctrl + Z` |
| 重做 | `⌘/Ctrl + Shift + Z` 或 `⌘/Ctrl + Y` |

撤销栈深 60 步。文本输入按"聚焦一次算一组"归并，不会一个字符一个字符撤。

### 2.6 预览 vs 编辑

顶栏有 `👁 预览` / `✎ 编辑` 切换按钮：

- **预览**：隐藏所有编辑框和手柄，看真实效果（改动都保留）
- **编辑**：显示编辑框，可继续改

### 2.7 图表怎么办

页面里的 Chart.js 图表**全自动处理**：

- 拖动区块后自动 resize，不会变形
- 撤销/重做后自动重建，不会空白
- 导出后图表配置完整保留

其他自定义 JS 图表（D3、ECharts、原生 canvas）不保证——如果发现拖动后坏了，用撤销退回，或者告诉我加适配。

---

## 3. 保存 / 导出

### 3.1 默认：下载干净 HTML

顶栏点 `⬇ 导出 HTML`（或 `⌘/Ctrl + S`），浏览器下载一份 `xxx.html`：

- **完全剥离编辑器**：所有 `data-he-ui`、`he-*` class、`contenteditable`、编辑器 UI DOM、注入脚本都被清干净
- **只保留你的真实改动**：文字、字体、字号、颜色、链接、图片、区块顺序
- **可直接替换原文件**或发给别人

### 3.2 覆盖保存（仅在 https/localhost 下可用）

如果副本是通过 `http://localhost:xxx` 或 `https://` 打开的，顶栏会有 `💾 保存` 按钮，用 File System Access API 原地覆盖。

`file://` 打开时**没有这个按钮**——这是浏览器安全策略，任何网页都不能静默改本地文件，无解。走「下载 → 替换」的通用姿势即可。

### 3.3 让 AI 帮你写回原文件

在对话里说「把导出的文件覆盖回原路径」，agent 会：

1. **先备份**原文件到 workspace
2. 用导出的干净 HTML 覆盖原路径
3. 验证覆盖成功

不会跳过备份这一步。

---

## 4. 常见问题

**Q：为什么某些文字点不进去改？**
A：可能是按钮、链接、tab 等交互元素，编辑器故意跳过；也可能是 `<script>` / `<style>` 内容（不该改）。想改交互元素的文字，可以复制整个区块再改副本。

**Q：拖拽时手柄不出现？**
A：鼠标要悬到"顶层区块"上——`.container/.page/main/body` 的直接子元素。深层嵌套的元素不会单独出手柄，避免误拖。要精细操作可以用 Alt+点选中，再用顶栏 ↑ ↓ 按钮。

**Q：改完导出后图表空白了？**
A：Chart.js 图表编辑器会自动重建，一般不会空白。如果是自定义 canvas 图表（D3/ECharts），可能需要在导出后手动重新触发一次绘图。撤销回退到编辑前，或者告诉我加适配。

**Q：字体换了导出后又变回去？**
A：编辑器把字体写成元素的内联 `style="font-family:..."`，导出即保留。如果变回去，说明原页面有 `!important` 规则盖过——把内联样式加上 `!important` 即可，或者告诉我修内核让编辑器默认加。

**Q：图片换完导出后打不开？**
A：编辑器把上传的图片转成 base64 内联，导出文件里直接嵌入。图片很大（>5MB）时导出文件会膨胀，但一定能打开。

**Q：这份 HTML 是 React/Vue 生成的，能用吗？**
A：**不能**。框架会持续重渲染 DOM，把你的手动改动冲掉。这个 skill 只针对**静态 HTML**（含少量原生 JS 或 Chart.js 是 OK 的）。

**Q：编辑到一半浏览器崩了怎么办？**
A：撤销栈在内存里，崩溃即丢。建议阶段性 `⌘/Ctrl + S` 导出中间版本。后续版本可以加 localStorage 自动保存。

**Q：能不能多人协作？**
A：v1 是单机版，导出的 HTML 就是最终产物。多人协作需要走 git 或者后续加个后端。

---

## 5. 反馈和贡献

发现问题或有功能建议：

- 记住复现步骤和你编辑的 HTML 特征（有没有图表？多少层嵌套？多少 KB？）
- 导出前用浏览器开发者工具看看 Console 有没有报错
- 把复现文件（去掉敏感信息后）发给维护者

想改内核：先读 [TECH_NOTES.md](TECH_NOTES.md)，里面有模块划分、设计原则、踩坑史和维护指南。
